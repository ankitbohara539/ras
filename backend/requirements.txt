# Web
fastapi>=0.116,<1.0
uvicorn[standard]>=0.35,<1.0
python-multipart>=0.0.20,<1.0

# Config
pydantic-settings>=2.10,<3.0
email-validator>=2.0,<3.0
python-dotenv>=1.1,<2.0

# Database
SQLAlchemy>=2.0,<3.0
alembic>=1.14,<2.0
psycopg2-binary>=2.9,<3.0
pgvector>=0.3,<1.0

# Supabase (auth + storage only)
supabase>=2.18,<3.0

# Classification and duplicate matching.
# scikit-learn only -- no torch. See app/ml/encoder.py for why, and for the
# TextEncoder seam that makes swapping in a transformer a one-line change.
scikit-learn>=1.5,<2.0
numpy>=2.0,<3.0
scipy>=1.14,<2.0
joblib>=1.4,<2.0

# Perceptual hashing for photo similarity.
Pillow>=10.0,<14.0
ImageHash>=4.3,<5.0

# Tests
pytest>=8.4,<9.0
httpx>=0.28,<1.0
