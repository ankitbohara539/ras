# Production requests stay on the frontend origin. Vercel forwards /api to
# Render using the rewrite in vercel.json, so browser CORS rules do not depend
# on Vercel's changing deployment URLs.
VITE_USE_API_PROXY=true
